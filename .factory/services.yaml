commands:
  install: npm install
  build: npm run build
  typecheck: npm run typecheck
  lint: npm run lint
  test: npm run test:unit
  knip: npm run knip
  clean: npm run clean

services:
  # No services needed for cleanup mission - it's code modification only
  # Extension is built and loaded into browser for testing
